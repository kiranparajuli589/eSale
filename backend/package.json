{
  "main": "tests/acceptance/index.js",
  "scripts": {
    "clean-all": "rimraf node_modules",
    "upgrade-all": "yarn upgrade",
    "install-all": "yarn install",
    "lint": "eslint tests --ext js --color",
    "lint-fix": "eslint tests --ext js --color --fix",
    "install": "cd node_modules/cucumber; yarn install --frozen-lockfile",
    "acceptance-tests": "./node_modules/.bin/cucumber-js --require tests/acceptance -f ./node_modules/cucumber-pretty",
    "selenium": "docker run --rm -d -p 127.0.0.1:${SELENIUM_PORT:-4444}:4444 -p 5900:5900 --name selenium selenium/standalone-chrome-debug"
  },
  "devDependencies": {
    "axios": "^0.19.2",
    "chromedriver": "^80.0.1",
    "cucumber": "^6.0.5",
    "cucumber-pretty": "^6.0.0",
    "eslint": "^6.8.0",
    "nightwatch": "^1.3.4",
    "nightwatch-api": "^3.0.1",
    "node-fetch": "^2.6.0"
  },
  "license": "ISC"
}
